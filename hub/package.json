{
  "name": "jens-wellness-hub",
  "version": "0.1.0",
  "private": true,
  "description": "One private wellness home for Jen and John. The Hub holds the shared foundation (login, profiles, privacy, heads-ups, gentle day mode) that every module plugs into.",
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "eslint && node scripts/check-copy.mjs",
    "typecheck": "tsc --noEmit && tsc --noEmit -p convex/tsconfig.json",
    "test": "node --test \"tests/**/*.test.ts\"",
    "check-copy": "node scripts/check-copy.mjs",
    "icons": "node scripts/icons.mjs"
  },
  "dependencies": {
    "@auth/core": "^0.37.0",
    "@convex-dev/auth": "^0.0.92",
    "convex": "^1.39.1",
    "lucide-react": "^0.475.0",
    "next": "16.2.6",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.6",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
