"use client";

import { CheckIn } from "@/modules/tend/screens/CheckIn";

/**
 * The shell's "How are you, really?" button lands here on every screen.
 * The button belongs to the shell; Tend supplies the screen behind it.
 */
export default function CheckInPage() {
  return <CheckIn />;
}
